# YouTuber App

A web-based application to visualize and explore YouTube statistics based on the number of subscribers for various YouTubers. This app provides an interactive visualization of YouTuber subscriber distributions and a searchable dataset.

## Features

- **Subscriber Distribution Visualization**: Displays a histogram showing the distribution of subscribers for YouTubers with less than 50 million subscribers.
- **Interactive Search**: Allows searching for YouTubers by name, filtering the data accordingly.
- **Dynamic Data Table**: Displays a table of YouTuber statistics, which updates as per the search input.

## Requirements

To run this app locally, you'll need the following Python libraries:

- `pandas` - For data manipulation
- `matplotlib` - For plotting and visualization
- `shiny` - For creating the web app interface
- `numpy` - For numerical operations

You can install the required libraries using the following command:

```bash
pip install -r requirements.txt
